services:
  horelac:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_PATH: /data/horelac.db
    volumes:
      - horelac-data:/data
    stop_grace_period: 20s
volumes:
  horelac-data:

